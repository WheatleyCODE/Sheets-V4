export enum ButtonColor {
  DEFAULT = 'default_color',
  PRIMARY = 'primary',
  SECONDARY = 'secondary',
  DANGER = 'danger',
}

export enum ButtonStyles {
  DEFAULT = 'default',
  CLEAR = 'clear',
}

export enum ButtonSize {
  BIG = 'big',
  NORMAL = 'normal',
  SMALL = 'small',
}

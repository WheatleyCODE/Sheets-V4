export enum ButtonColor {
  DEFAULT = 'default_color',
  PRIMARY = 'primary',
}

export enum ButtonStyles {
  DEFAULT = 'default',
  CLEAR = 'clear',
}

export enum ButtonSize {
  BIG = 'big',
  NORMAL = 'normal',
  SMALL = 'small',
}
